{
  "name": "vistara-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --project=chromium --grep @smoke --forbid-only",
    "install:browsers": "playwright install chromium firefox webkit",
    "install:smoke-browser": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "26.4.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": "^22.13.0 || >=24.0.0"
  }
}
